services:
  migrate:
    image: node:20-alpine
    restart: 'no'
    working_dir: /app
    volumes:
      - .:/app
    environment:
      DATABASE_URL: file:./local.db
    command: sh -c "npm install && npm run db:push"
